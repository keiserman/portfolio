---
const { text, variant, url, ...rest } = Astro.props;
const isSecondary = variant === "secondary";
---

<a
  href={url}
  class={`p-3 rounded-lg inline-flex self-start items-center gap-2 font-mono text-xs uppercase leading-none tracking-wider shadow-sm transition ${
    isSecondary
      ? "bg-transparent text-zinc-100 border border-zinc-700 hover:bg-zinc-800"
      : "bg-white text-zinc-950 border-none hover:bg-zinc-200"
  }`}
  data-value={text}
  {...rest}
>
  {text}
  <slot />
</a>

<script>
  const letters = "ABCDEFGHIJKLMNOPQRSTUVWXYZ";

  function scrambleText(button: HTMLElement & { _interval?: ReturnType<typeof setInterval> }) {
    let iteration = 0;
    const originalText = (button as HTMLElement).dataset.value ?? "";
    let interval = setInterval(() => {
      button.textContent = originalText
        .split("")
        .map((letter, index) => {
          if (index < iteration) return originalText[index];
          return letters[Math.floor(Math.random() * 26)];
        })
        .join("");
      if (iteration >= originalText.length) clearInterval(interval);
      iteration += 1 / 3;
    }, 30);
    button._interval = interval;
  }

  function restoreText(button: HTMLElement & { _interval?: ReturnType<typeof setInterval> }) {
    clearInterval(button._interval);
    button.textContent = (button as HTMLElement).dataset.value ?? "";
  }

  document.querySelectorAll<HTMLElement>("a[data-value]").forEach((button) => {
    button.addEventListener("mouseover", () => scrambleText(button as any));
    button.addEventListener("mouseleave", () => restoreText(button as any));
  });
</script>
