---
const { title, text, ...rest } = Astro.props;
---

<div class="card flex flex-col gap-6" {...rest}>
  <slot />
  <div class="text-2xl">{title}</div>
  <p class="text-base text-neutral-600 dark:text-neutral-400">{text}</p>
</div>
